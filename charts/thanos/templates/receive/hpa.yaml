{{- if and .Values.receive.enabled .Values.receive.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "thanos.compName" (list . "receive") }}
  labels:
{{ include "thanos.labels" . | indent 4 }}
  annotations:
    {{- include "thanos.annotations" (dict "Values" .Values "component" "receive") | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: {{ .Values.receive.autoscaling.targetKind | default "StatefulSet" }}
    name: {{ include "thanos.compName" (list . "receive") }}
  minReplicas: {{ .Values.receive.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.receive.autoscaling.maxReplicas }}
  metrics:
  {{- $cpu := .Values.receive.autoscaling.targetCPUUtilizationPercentage }}
  {{- $mem := .Values.receive.autoscaling.targetMemoryUtilizationPercentage }}
  {{- if or $cpu $mem }}
    {{- if $cpu }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ $cpu }}
    {{- end }}
    {{- if $mem }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ $mem }}
    {{- end }}
  {{- else }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
  {{- end }}
{{- end }}
