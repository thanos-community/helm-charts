{{- if and .Values.storegateway.enabled .Values.storegateway.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "thanos.compName" (list . "storegateway") }}
  labels:
{{ include "thanos.labels" . | indent 4 }}
  annotations:
    {{- include "thanos.annotations" (dict "Values" .Values "component" "storegateway") | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: {{ .Values.storegateway.autoscaling.targetKind | default "StatefulSet" }}
    name: {{ include "thanos.compName" (list . "storegateway") }}
  minReplicas: {{ .Values.storegateway.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.storegateway.autoscaling.maxReplicas }}
  metrics:
  {{- $cpu := .Values.storegateway.autoscaling.targetCPUUtilizationPercentage }}
  {{- $mem := .Values.storegateway.autoscaling.targetMemoryUtilizationPercentage }}
  {{- if or $cpu $mem }}
    {{- if $cpu }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ $cpu }}
    {{- end }}
    {{- if $mem }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ $mem }}
    {{- end }}
  {{- else }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80
  {{- end }}
{{- end }}
